const Insider = ({ data }) => {
  console.log("======>>>>>>ITEM", data);
  return (
  
      <div className="">
        {/* {data.datalist.map((item, index) => {
          return (
            <span className="text-[14px] font-semibold" key={index}>
              
           
          </span>
          )
              
          
          })}
         */}
         i am Insider
      </div>
      

  );
  // console.log("hiiiiiiiiii",_item.title);
};

export default Insider;
